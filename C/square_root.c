#include <stdio.h>
#include <stdint.h>
#include <math.h>

int square_root(unsigned int value){
    return sqrt(value);
}

int main(int argc, char **argv){
    int resp = square_root(65025);
    //255, square_root(65025)
}
